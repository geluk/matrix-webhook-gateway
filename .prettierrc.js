module.exports = {
  // Left over from tslint configuration, I'd rather not change this because
  // it would lead to massive changes in every file.
  singleQuote: true,
  // See above. In addition, enforcing trailing commas everywhere makes diffs
  // a bit nicer to look at.
  trailingComma: 'all',
};
